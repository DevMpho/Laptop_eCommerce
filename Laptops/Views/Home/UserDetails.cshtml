<!-- Laptops.Models.UserDetails

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>User Details</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background-color: #e0f2ec;
            padding: 20px;
        }

        .container {
            background-color: #c5d9bd;
            padding: 30px;
            border-radius: 12px;
            max-width: 800px;
            margin: auto;
        }

        h1, h2 {
            text-align: center;
        }

        .form-group {
            background-color: #f0fff0;
            border: 2px solid #8dc78a;
            margin: 10px 0;
            padding: 10px;
            border-radius: 6px;
        }

        .form-group strong {
            display: inline-block;
            width: 200px;
        }

        .btn {
            display: block;
            width: fit-content;
            margin: 30px auto 0;
            padding: 10px 20px;
            background-color: #4CAF50;
            color: white;
            border-radius: 8px;
            text-decoration: none;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>User Details</h1>
        <h2>@Model.Name</h2>

        <div class="form-group"><strong>Email:</strong> @Model.Email</div>
        <div class="form-group"><strong>Phone Number:</strong> @Model.PhoneNumber</div>
        <div class="form-group"><strong>Purchase Date:</strong> @Model.PurchaseDate.ToString("dd/MM/yyyy")</div>
        <div class="form-group"><strong>Payment Status:</strong> @Model.PaymentStatus</div>
        <div class="form-group"><strong>Purchase Status:</strong> @Model.PurchaseStatus</div>
        <div class="form-group"><strong>Order Number:</strong> @Model.OrderNumber</div>

        <hr />

        <h2>Laptop Specs</h2>
        <div class="form-group"><strong>Model:</strong> @Model.Model</div>
        <div class="form-group"><strong>Processor:</strong> @Model.Processor</div>
        <div class="form-group"><strong>RAM:</strong> @Model.RAM</div>
        <div class="form-group"><strong>Storage:</strong> @Model.Storage</div>
        <div class="form-group"><strong>Graphics:</strong> @Model.Graphics</div>
        <div class="form-group"><strong>Operating System:</strong> @Model.OperatingSystem</div>

        <a class="btn" href="/">← Back to Home</a>
    </div>
</body>
</html>-->
